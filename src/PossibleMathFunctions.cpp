#include "PossibleMathFunctions.h"

double** PossibleMathFunctions::DCT8x8(double** data)
{
	double** newData = new double* [8];
	for (int i = 0; i < 8; i++)
	{
		newData[i] = new double[8];
	}

	//Take the 2D DCT. Assume that the values are un-normalized

	return nullptr;
}
